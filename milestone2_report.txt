The Big-O time complexity for enqueue and dequeue in our LinkedQueue is O(1) for both. 
This is because we implemented a head and tail attribute, and we treat enqueue as add_last() and
dequeue as remove_first(). This means that only an assignment operation is needed to enqueue and
dequeue (either move the head foward, or add a new tail node) which causes the time complexity to be 
O(1)

The Big-O time complexity for bubble sort is O(n^2). This is because in an unsorted list, the bubble sort
will still have to 

The Big-O time complexity for insertion sort is O(n^2). This is because in an unsorted list, 

The Big-O time complexity for selection sort is O(n^2). This is because in an unsorted list, 


The Big-O time complexity for recursive binary sort is log(n) because the amount of
iterations made is 1/2 the full list. Sorting is required for correctness because binary search 
operates under the assumption that the current item will be in the correct position, enabling the 
conditional of target greater than or less than the current element to be evaluated correctly.
If the list was unsorted, then the binary search wouldn't work because it's main assumption
would be false.

Between bubble sort, insertion sort, and selection sort, 
